#!/bin/bash

BUCKET="<%= @bucket %>"

echo "uploading file $1"
aws s3 cp $1 s3://${BUCKET}/upload/
if [ $? -eq 0 ] ; then
	echo deleting file $1
	sudo rm $1
	echo "$1 uploaded"
else
	echo "Failed to upload $1"
fi

